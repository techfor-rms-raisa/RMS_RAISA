/**
 * CONSTANTS INDEX
 * Exportação centralizada de todas as constantes
 * 
 * Uso: import { ROUTES, RISK_COLORS, APP_TITLE } from '@/constants';
 */

// Re-export de routes
export * from './routes';

// Re-export do constants original (movido para esta pasta)
// Por enquanto, o constants.ts está na raiz, então não re-exportamos aqui
// Futuramente: mover constants.ts para constants/app.ts e re-exportar
